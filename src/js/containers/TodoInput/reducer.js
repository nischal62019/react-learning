import { CREATE } from './constants'

export const TodoInputReducer = (state = { inputText: '' }, action) => {
    switch (action.type) {
       
        default:
            break;
    }
    return state
}